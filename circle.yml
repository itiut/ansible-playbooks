machine:
  services:
    - docker
dependencies:
  pre:
    - docker info
    - docker build -t itiut/ansible-playbooks-test ./tests
test:
  override:
    - docker run -t -v $(pwd):/ansible-playbooks -w /ansible-playbooks itiut/ansible-playbooks-test ./tests/test.sh
